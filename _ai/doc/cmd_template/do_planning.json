{
  "task": "brainstorm then create a planning for next task",
  "save_to": "create _ai/task_16.md",
  "resources": {
    "user-onboarding-example": "packages/examples/src/user-onboarding/workflow.json"
  },
  "mcp": {},
  "target": [
    {
      "goal": "enhance packages/ts-generator",
      "requirements": [
        "check the packages/core/src/types.ts. Where we have globalState which contain a default value comply with globalStateSchema. And for each task we also have paramsSchema and params",
        "let update the generator consumed it and generate source code & types"
      ]
    }
  ],
  "steps": {
    "1": "read resources, use context7 mcp when need",
    "2": "read current source code",
    "3": "create a task_16 to achieve goal"
  }
}
