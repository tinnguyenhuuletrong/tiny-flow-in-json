{
  "task": "brainstorm then create a planning for next task",
  "save_to": "create _ai/task_19.md",
  "resources": {
    "ref_current_connection": "_ai/doc/ref/ref_current_connection.png"
  },
  "mcp": {},
  "target": [
    {
      "goal": "enhance packages/web",
      "requirements": [
        "Enhance control for edge handle especially for FlowView - DecisionNode",
        "current limitations",
        "- Edge handle for node is fixed - see packages/web/src/app/components/custom-nodes/DecisionNode.tsx",
        "enhance:",
        "- We can drag and drop handle along the step shape ( minimum distance between each handle should keep and adjust when drop )"
      ],
      "note": [
        "The position of each handle x y should store into node metadata",
        "Clear state on handle indicator when drag , drop handle. handle position should shit on the Node border line"
      ]
    }
  ],
  "steps": {
    "1": "read resources, use context7 mcp when need",
    "2": "read current source code",
    "3": "create a task_19 to achieve goal"
  }
}