{
  "task": "brainstorm then create a planning for next task",
  "save_to": "create _ai/task_21.md",
  "resources": {
    "web": "packages/web"
  },
  "mcp": {},
  "target": [
    {
      "goal": "enhance packages/web",
      "requirements": [
        "Currently only DecisionNode support connection layout metadata?.handles",
        "Let try to convert it into a common component and reuse in all others node type ( no need for Begin and End )",
        "Review the HandleEditor make sure it work well for all others node type"
      ],
      "note": [
        "Nice to have to scan src, list all metadata for node( aka step ) and create typeDef for this "
      ]
    }
  ],
  "steps": {
    "1": "read resources, use context7 mcp when need",
    "2": "read current source code",
    "3": "create a task_21 to achieve goal. Task should specific and detail. So Junior AI Agent can work correctly"
  }
}
