{
  "task": "brainstorm then create a planning for next task",
  "save_to": "create _ai/task_18.md",
  "resources": {},
  "mcp": {},
  "target": [
    {
      "goal": "enhance packages/core",
      "requirements": [
        "Add 2 new step type",
        "- resumeAter. Resume the flow after a time",
        "   - params a duration string. where are humanize date time string",
        "- waitForEvent. Resume the flow after a time",
        "   - params contain 2 parts. eventInput, eventOutput. each Input and Output contain a jsonSchema and associated value"
      ],
      "note": [
        "read the file packages/core/src/types.ts. Also update description + variable name.",
        "Should consider DX. avoid confuse between different step type"
      ]
    }
  ],
  "steps": {
    "1": "read resources, use context7 mcp when need",
    "2": "read current source code",
    "3": "create a task_18 to achieve goal"
  }
}
