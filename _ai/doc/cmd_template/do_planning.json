{
  "task": "brainstorm then create a planning for next task",
  "save_to": "create _ai/task_04.md",
  "resources": {
    "requirement.md": "_ai/doc/requirement.md"
  },
  "mcp": {
    "context7": "for fetching  Fetches up-to-date documentation for a library"
  },
  "target": {
    "goal": "polish the code view",
    "note": [
      "build json schema from zod",
      "inject json schema to monaco editor for better code auto complete",
      "add schema validation before save and applied to store",
      "in case failed -> display error ( try to explore can we display error natively in monaco editor. if not it is ok to display outside )"
    ]
  },
  "steps": {
    "1": "read resources, use context7 when need",
    "2": "read current source code",
    "3": "create a task_04 to achieve goal",
    "4": "waiting for human review and confirm before start to code"
  }
}